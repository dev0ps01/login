@Library('todoapp') _

todo (
        COMPONENT             : 'login',
        PROJECT_NAME          : "Todoapp",
        SLAVE_LABEL           : "GOLANG",
        SKIP_NEXUS_UPLOAD     : false,
        APP_TYPE              : "GOLANG"
)